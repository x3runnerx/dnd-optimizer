import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.d20companion.app',
  appName: 'D20 Campaign Companion',
  webDir: 'dist/public',
  android: {
    backgroundColor: '#1a1410'
  }
};

export default config;
